package com.meeple.shared.frame.window;

import org.lwjgl.glfw.GLFW;
@Deprecated
public class WindowCallbackManager {

	public void setWindowCallbacks(long windowID, MirroredWindowCallbacks callbacks) {
		/*if (callbacks != null) {
			if (callbacks.charCallback != null)
				GLFW.glfwSetCharCallback(windowID, callbacks.charCallback);
			if (callbacks.charModsCallback != null)
				GLFW.glfwSetCharModsCallback(windowID, callbacks.charModsCallback);
			if (callbacks.cursorEnterCallback != null)
				GLFW.glfwSetCursorEnterCallback(windowID, callbacks.cursorEnterCallback);
			if (callbacks.cursorPosCallback != null)
				GLFW.glfwSetCursorPosCallback(windowID, callbacks.cursorPosCallback);
			if (callbacks.dropCallback != null)
				GLFW.glfwSetDropCallback(windowID, callbacks.dropCallback);
			if (callbacks.frameBufferSizeCallback != null)
				GLFW.glfwSetFramebufferSizeCallback(windowID, callbacks.frameBufferSizeCallback);
			if (callbacks.keyCallback != null)
				GLFW.glfwSetKeyCallback(windowID, callbacks.keyCallback);
			if (callbacks.mouseButtonCallback != null)
				GLFW.glfwSetMouseButtonCallback(windowID, callbacks.mouseButtonCallback);
			if (callbacks.scrollCallback != null)
				GLFW.glfwSetScrollCallback(windowID, callbacks.scrollCallback);
			if (callbacks.windowCloseCallback != null)
				GLFW.glfwSetWindowCloseCallback(windowID, callbacks.windowCloseCallback);
			if (callbacks.windowContentScaleCallback != null)
				GLFW.glfwSetWindowContentScaleCallback(windowID, callbacks.windowContentScaleCallback);
			if (callbacks.windowFocusCallback != null)
				GLFW.glfwSetWindowFocusCallback(windowID, callbacks.windowFocusCallback);
			if (callbacks.windowIconifyCallback != null)
				GLFW.glfwSetWindowIconifyCallback(windowID, callbacks.windowIconifyCallback);
			if (callbacks.windowMaximizeCallback != null)
				GLFW.glfwSetWindowMaximizeCallback(windowID, callbacks.windowMaximizeCallback);
			if (callbacks.windowPosCallback != null)
				GLFW.glfwSetWindowPosCallback(windowID, callbacks.windowPosCallback);
			if (callbacks.windowRefreshCallback != null)
				GLFW.glfwSetWindowRefreshCallback(windowID, callbacks.windowRefreshCallback);
			if (callbacks.windowSizeCallback != null)
				GLFW.glfwSetWindowSizeCallback(windowID, callbacks.windowSizeCallback);
		
		}*/
	}

}
